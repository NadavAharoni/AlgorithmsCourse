# Claude Instructions

## Workflow Preferences
- For small changes: edit files directly, do NOT commit
- Only commit when explicitly asked
- Preserve newlines in code (user adds them intentionally for clarity)
