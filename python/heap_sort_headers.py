def is_max_heap(arr, i=0, key=lambda x: x):
    """
    Check if the array is a valid max heap starting from index i.
    Verifies that each node from i onwards is not greater than its parent.
    """

    # Exercise: imlement this function
    return

